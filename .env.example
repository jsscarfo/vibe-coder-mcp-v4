# OpenRouter Configuration
## Specifies your unique API key for accessing OpenRouter services. 
## Replace with your actual key obtained from OpenRouter.ai.
OPENROUTER_API_KEY="your_openrouter_api_key_here"

## Defines the base URL for the OpenRouter API endpoints. 
## The default value is usually correct and should not need changing unless instructed otherwise.
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

## Sets the specific model to be used via OpenRouter for certain AI tasks.
GEMINI_MODEL=openrouter/quasar-alpha

# Optional Configuration
## Sets the directory where generated files are saved (default is VibeCoderOutput/ in project directory)
# VIBE_CODER_OUTPUT_DIR=/absolute/path/to/desired/output/directory

## Sets the logging level (debug, info, warn, error)
# LOG_LEVEL=info

## Specifies the runtime environment (production or development)
# NODE_ENV=production